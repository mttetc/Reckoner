"""Path of Exile 2 adapter — Phase 2 (SPEC § 9).

Deliberately empty. The measure of the architecture is that filling this package, plus one
registry line, adds PoE2 without touching ``app.domain`` (SPEC § 8).
"""
