services:
  db:
    image: pgvector/pgvector:pg17
    environment:
      POSTGRES_USER: reckoner
      POSTGRES_PASSWORD: reckoner
      POSTGRES_DB: reckoner
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
volumes:
  pgdata:
